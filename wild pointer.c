#include<stdio.h>
main()
{
	int *ptr; // here pointer has not value
	int *ptrr ; // it is same as null 
	
	printf(" The value at ptr : %d", *ptr);
 	printf(" The address at ptr : %d", ptr);	
	
}
